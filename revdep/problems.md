# BayesianNetwork

Version: 0.1.5

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpUhbRi6/Rd2pdf1c4c79bd2898'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

*   checking dependencies in R code ... NOTE
    ```
    Namespaces in Imports field not imported from:
      'shinytest' 'testthat'
      All declared Imports should be used.
    ```

# bs4Dash

Version: 0.2.0

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpMxXaUi/Rd2pdf575c710b2ce2'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

# discoveR

Version: 1.0.1

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpQl3g1q/Rd2pdf2d903a7619ac'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

# dplyrAssist

Version: 0.1.0

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpaY4quI/Rd2pdf137c6a0a7183'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

*   checking dependencies in R code ... NOTE
    ```
    Namespaces in Imports field not imported from:
      'tidyr' 'tidyverse'
      All declared Imports should be used.
    ```

# eAnalytics

Version: 0.1.4

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpUpyEti/Rd2pdf5f304fdf2cbf'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

# esquisse

Version: 0.1.7

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpYdWOr8/Rd2pdf203c36dd54c2'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

# getTBinR

Version: 0.5.6

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpIj6WlQ/Rd2pdf686850302'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

# ggplotAssist

Version: 0.1.3

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpAVNU2V/Rd2pdf4a2871fb33f8'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

*   checking dependencies in R code ... NOTE
    ```
    Namespaces in Imports field not imported from:
      'gcookbook' 'ggthemes' 'moonBook' 'tidyverse'
      All declared Imports should be used.
    ```

# gimmeTools

Version: 0.1

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpwDdWGk/Rd2pdf606825e4512'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

# MtreeRing

Version: 1.1

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/Rtmpi2F1jZ/Rd2pdf32a0b863de8'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

# plethem

Version: 0.1.7

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/Rtmp4ii0sj/Rd2pdf6d2869c83e79'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

*   checking dependencies in R code ... NOTE
    ```
    Namespaces in Imports field not imported from:
      'V8' 'devtools' 'formatR' 'gdata' 'rhandsontable' 'shinythemes'
      'sqldf'
      All declared Imports should be used.
    ```

# powdR

Version: 0.2.0

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/Rtmp2vFukt/Rd2pdf59a8daa5300'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

*   checking dependencies in R code ... NOTE
    ```
    Namespace in Imports field not imported from: 'shinyWidgets'
      All declared Imports should be used.
    ```

# shinydashboardPlus

Version: 0.6.0

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/Rtmp8QLPgP/Rd2pdf5234450e3d4b'
    ```

*   checking re-building of vignette outputs ... WARNING
    ```
    ...
     * 0x57adbf8	0x49f0aba C:\Softs\RStudio\bin\pandoc\pandoc.exe+0x45f0aba
     * 0x57adc00	0x7879ab8
     * 0x57adc08	0x78722f3
     * 0x57adc10	0x79d7378
     * 0x57adc18	0x78ff700
     * 0x57adc20	0x78ff718
     * 0x57adc28	0x78ff730
     * 0x57adc30	0x4b616f1 C:\Softs\RStudio\bin\pandoc\pandoc.exe+0x47616f1
     * 0x57adc38	0x4231e2a C:\Softs\RStudio\bin\pandoc\pandoc.exe+0x3e31e2a
     * 0x57adc40	0x4b616f1 C:\Softs\RStudio\bin\pandoc\pandoc.exe+0x47616f1
     * 0x57adc48	0x4a6d3e9 C:\Softs\RStudio\bin\pandoc\pandoc.exe+0x466d3e9
     * 0x57adc50	0x4b616f1 C:\Softs\RStudio\bin\pandoc\pandoc.exe+0x47616f1
     * 0x57adc58	0x4b616f1 C:\Softs\RStudio\bin\pandoc\pandoc.exe+0x47616f1
     * 0x57adc60	0x41a98d3 C:\Softs\RStudio\bin\pandoc\pandoc.exe+0x3da98d3
     * 0x57adc68	0x4280a19 C:\Softs\RStudio\bin\pandoc\pandoc.exe+0x3e80a19
    
       ... (maximum recursion depth reached.)
    
    Erreur : le traitement de la vignette 'boxPlus-sidebar.Rmd' a échoué avec le diagnostic :
    pandoc document conversion failed with error 11
    Exécution arrêtée
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

# visNetwork

Version: 2.0.5

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpgHNylS/Rd2pdf4e9068371288'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

*   checking installed package size ... NOTE
    ```
      installed size is 11.0Mb
      sub-directories of 1Mb or more:
        doc           5.6Mb
        htmlwidgets   3.9Mb
    ```

# wiseR

Version: 1.0.1

## In both

*   checking PDF version of manual without hyperrefs or index ... ERROR
    ```
    Re-running with no redirection of stdout/stderr.
    Hmm ... looks like a package
    You may want to clean up by 'rm -Rf C:/Users/perri/AppData/Local/Temp/RtmpyYLQ8I/Rd2pdf3bf8778f60de'
    ```

*   checking PDF version of manual ... WARNING
    ```
    LaTeX errors when creating PDF version.
    This typically indicates Rd problems.
    ```

*   checking installed package size ... NOTE
    ```
      installed size is  7.1Mb
      sub-directories of 1Mb or more:
        bn   7.1Mb
    ```

*   checking dependencies in R code ... NOTE
    ```
    Namespaces in Imports field not imported from:
      'DT' 'DescTools' 'HydeNet' 'RBGL' 'Rgraphviz' 'arules' 'bnlearn'
      'dplyr' 'graph' 'igraph' 'linkcomm' 'missRanger' 'parallel' 'psych'
      'rhandsontable' 'rintrojs' 'shinyBS' 'shinyWidgets' 'shinyalert'
      'shinycssloaders' 'shinydashboard' 'tools' 'visNetwork'
      All declared Imports should be used.
    ```

